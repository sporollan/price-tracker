apiVersion: v1
kind: ConfigMap
metadata:
  name: scraper-configmap
data:
  POSTGRES_DB: users
  POSTGRES_USER: ps_user
---
apiVersion: v1
kind: Service
metadata:
  name: scraper-service
spec:
  selector:
    app: scraper-service
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scraper-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scraper-service
  template:
    metadata:
      labels:
        app: scraper-service
    spec:
      containers:
      - name: scraper-service
        image: scraper-service:dev
        imagePullPolicy: Never
        ports:
        - containerPort: 8001
        resources:
          requests:
            cpu: "1"
            memory: "2Gi"
          limits:
            cpu: "2"
            memory: "4Gi"
        envFrom:
          - configMapRef:
              name: app-config
          - configMapRef:
              name: scraper-configmap
---
apiVersion: v1
kind: Service
metadata:
  name: scraper-db
spec:
  selector:
    app: scraper-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scraper-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scraper-db
  template:
    metadata:
      labels:
        app: scraper-db
    spec:
      containers:
      - name: scraper-db
        image: postgres
        ports:
        - containerPort: 5432
        resources:
          requests:
            memory: "256Mi"
            cpu: "500m"
          limits:
            memory: "1024Mi"
            cpu: "1"     # Max 1 CPU core
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: scraper-secrets
              key: POSTGRES_PASSWORD
        volumeMounts:
        - name: postgresqldata1
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgresqldata1
        persistentVolumeClaim:
          claimName: postgresqldata1
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresqldata1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi